# vendored from skill-creator (scripts/ and eval-viewer/) — Windows-clean harness pieces
# These are copied verbatim (only relocated imports adjusted) so the agent-architect eval
# phase never depends on skill-creator being installed.
